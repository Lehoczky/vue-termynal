{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "selector-class-pattern": null
  },
  "overrides": [
    {
      "files": ["playground/**/*.css"],
      "rules": {
        "at-rule-no-unknown": [
          true,
          {
            "ignoreAtRules": ["tailwind", "apply", "layer"]
          }
        ]
      }
    }
  ]
}
